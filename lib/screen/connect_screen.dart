import 'package:flutter/material.dart';

class Connect_screen extends StatelessWidget {
  const Connect_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Connect Screen"),
    );
  }
}

